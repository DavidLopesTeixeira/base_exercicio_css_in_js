import { FormHero, HeroTitle, HeroContainer } from './styles'

const Hero = () => (
  <FormHero>
    <HeroContainer>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroContainer>
  </FormHero>
)

export default Hero
